# 그로스하이 정책 자동화 로드맵 (ROADMAP)

> 이 문서는 **growthhigh-crawler**와 **growthhigh-policy 양쪽 레포 루트에 동일하게** 둔다.
> 전체 자동화의 **기준 문서** — 작업하면서 "현재 위치"(§6)를 계속 갱신한다.
> 최종 갱신: 2026-07-04

---

## 1. 최종 그림

**크롤링 → 검토서 → 가공**이 **매일 자동으로 도는** 파이프라인.
정부지원사업 공고를 매일 수집·정제해 정책 DB에 쌓고(①), 그중 필요한 건 그로스하이 톤의
검토서로 만들고(②), 이를 **클라이언트 큐레이션 / 매일 브리핑 / 주간 뉴스레터**로 가공해 전달(③)한다.

**사람 개입은 딱 두 곳:**
1. **업종·유형 판정** — 확실한 건 기계가 자동, 애매한 건 사람이 확정 (§5).
2. **발행 확인** — 검토서·뉴스레터 등 대외 산출물이 나가기 전 최종 컨펌.

나머지(수집·정제·중복대조·변환·전달)는 전부 자동.

---

## 2. 3층 구조

| 층 | 이름 | 하는 일 | 레포 |
|---|---|---|---|
| **①** | 크롤링 | 18개 사이트 공고 수집 → 검수·정규화·중복대조·업종태깅 → 정책 DB 적재 | growthhigh-crawler |
| **②** | 검토서 | 공고문 PDF → 그로스하이 톤 HTML 검토서 + 미팅용 인덱스 | growthhigh-policy |
| **③** | 가공 | DB·검토서 → 클라이언트 큐레이션 / 매일 브리핑 / 주간 뉴스레터 | (예정) |

---

## 3. 3단계 진행 방식

- **1단계 — 조각 완성**: 각 조각을 손으로 돌리며 안정화. ← **지금 여기**
- **2단계 — 조각 연결**: 조각끼리 이어 반자동으로 (①→② 자동 넘김 등).
- **3단계 — 오케스트레이션**: Make/n8n으로 매일 정해진 시각에 전체 자동 실행.

---

## 4. 도구 선택 기준 — 판단 vs 실행

- **판단은 Claude**: 업종/유형 분류, 검토서 본문 작성, 요약, 큐레이션 선별 등
  "읽고 이해해서 결정"하는 일.
- **실행·연결·전달은 Make/n8n**: 스케줄 트리거, 단계 간 데이터 전달, 노션·메일·스티비 연동 등
  "정해진 걸 나르는" 일.
- 새 기능을 붙일 때 **이 구분으로 툴을 고른다**. (판단이면 Claude 스킬/코드, 배관이면 Make/n8n)

---

## 5. 업종 판정 — 2단계 방식 (2026-07-04 채택)

제목·요약만으론 업종이 부정확해서(예: '하드웨어'→제조로 오판, '컨설팅'→지식으로 오판),
판정을 두 단계로 나눈다.

1. **크롤링 단계 (제목·요약만)**: 사례집·키워드로 **확실한 것만** 확정.
   애매하면 억지로 안 채우고 **"미정(pending)"** 표시만 한다. (자동 추측 금지)
2. **검토서 단계 (공고문 PDF 전체)**: 검토서 만들 때 원문을 읽으므로 그때 업종 확정 →
   DB의 그 공고를 **미정 → 확정**으로 업데이트. 여기서도 애매하면 **사람에게 확인**,
   **사람 판정만 사례집(industry_casebook)에 학습**.
3. **검토서 안 만드는 공고**: 미정으로 남겨두고, 나중에 큐레이션 필요할 때 미정 것만 모아 처리.

이 방식은 **공용 모듈에 반영해 모든 사이트에 공통 적용**한다.

---

## 6. 현재 위치 (2026-07-05)

### ① 크롤링 — 18개 중 **3개 완료**
- ✅ 기업마당 · K-Startup · NIPA (공용 처리계층 `policy_common` 위에)
- ⬜ 나머지 **15개** 사이트
- ✅ 업종 2단계 1단계(확실만 확정/애매는 미정) 공용화 완료. 사용자 확정분 사례집 학습 작동.

### ② 검토서 — **파이프라인 완성**
- ✅ PDF→HTML 검토서 + 인덱스 자동화 (다운로더·오케스트레이터·워크리스트)
- ✅ 검토서 **톤 통일**(헤더 로고) — 정본 `_template.html` 확립(스킬이 이걸 복제),
  171~183 이미지 로고 반영·라이브 통일 완료. 100~170 69건 텍스트로고는 선택 과제.
- ⬜ 업종 2단계의 **"검토서 단계 확정(미정→확정)"** 반영 (다음 단계)

### ③ 가공 — **미착수**
- ⬜ 클라이언트 큐레이션 / 매일 브리핑 / 주간 뉴스레터

### 오케스트레이션 (Make/n8n) — **미착수**

---

## 7. 다음 걸음

1. **업종 2단계 분류 공용화** — 크롤링=확실만 확정·애매는 미정, 검토서=미정→확정.
   `policy_common`에 반영해 전 사이트 공통 적용.
2. **검토서 톤 통일** — 헤더 로고 등 배치 전체 톤 맞추기.
3. **크롤러 사이트 확장** — 남은 15개 사이트 앞단 추가(공용 처리계층 재사용).
